apiVersion: v1
kind: Service
metadata:
  name: k3s-service
spec:
  type: NodePort
  selector:
    app: k3s-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000